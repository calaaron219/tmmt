# Handoff — 2026-04-23 (Phase 1 complete)

> Read this after `CLAUDE.md` for current state. For architecture and conventions, see `CLAUDE.md`.

## Project identity

TMOS is an event-driven AI agent system for personal finance + time management. Core loop: `Event → Agent → Decision → Action → Log`. Full plan at `~/.claude/plans/golden-percolating-dewdrop.md`.

Live at https://tmmt-web.vercel.app (login required). Backend API at https://tmmt-production.up.railway.app/ (currently "Hello World!", unused until Phase 4).

## What's shipped

**Phase 0 — Foundations** ✅
- Monorepo scaffolded (Next.js 16 + NestJS 11 + Prisma + shared packages)
- Vercel + Railway deployed
- GitHub Projects board + 18 labels + 8 phase epic issues
- CI: lint + typecheck + prisma validate

**Phase 1 — Foundation Backend + Money CRUD** ✅
- PR #9 (schema) — 11 Prisma tables + 5 enums, migration applied to Railway
- PR #10 (auth) — Auth.js with GitHub + Google OAuth, JWT sessions, protected `/app/*`
- PR #11 (transactions) — Server Actions + rule-based categorizer + `/app/money` UI
- PR #12 (categories) — full add/edit/delete UI at `/app/money/categories`
- PR #13 (CSV import) — drag-drop, auto-detect columns, batch insert, rules-categorize
- PR #14 (UX polish) — readability, typography, placeholder contrast

**Verified working:**
- [x] Login with GitHub end-to-end
- [x] Add/delete transactions via UI
- [x] Manage categories (add/edit/delete with color swatches, emoji)
- [x] Import CSV, auto-categorize with rules
- [x] Mobile layout works at 375px
- [x] Prisma Studio inspection matches UI state

## What's in progress

**Nothing.** Phase 1 is 100% shipped. No open PRs, no blocked work. Clean main branch ready for Phase 2.

This handoff PR itself (`docs/handoff-and-claude-md`) is the only thing pending: adds `CLAUDE.md` + this doc.

## Repo state

- **Main branch head:** PR #14 merged (`18c2a2d feat(money): ... (#13)` + PR #14's readability commit)
- **Open PRs:** none (this handoff PR is next)
- **Open branches:** `docs/handoff-and-claude-md` (this one)
- **Production:** `https://tmmt-web.vercel.app` — Phase 1 feature-complete
- **Database:** 1 migration applied (`20260422230302_phase_1_initial`), contains user's real test data

## Critical files (state as of today)

```
apps/web/src/
├── auth.ts                        # Auth.js config with GH + Google
├── proxy.ts                       # Route guard for /app/*
├── app/
│   ├── page.tsx                   # Landing → /app if logged in
│   ├── login/page.tsx             # Provider buttons
│   ├── app/
│   │   ├── layout.tsx             # Header + sign-out
│   │   ├── page.tsx               # Module cards (Money + Time-stub)
│   │   └── money/
│   │       ├── page.tsx           # List + quick-add
│   │       ├── actions.ts         # 8 Server Actions (see below)
│   │       ├── quick-add-form.tsx
│   │       ├── transaction-list.tsx
│   │       ├── categories/        # PR #12
│   │       └── import/            # PR #13
│   └── api/auth/[...nextauth]/route.ts
├── lib/
│   ├── categorizer.ts             # Rule-based matcher (Phase 2 adds AI fallback here)
│   └── default-categories.ts      # Seeds 11 categories on first action

packages/
├── db/
│   ├── prisma/schema.prisma       # 11 tables
│   ├── prisma/migrations/20260422230302_phase_1_initial/
│   └── src/client.ts              # Prisma singleton
└── shared/
    └── src/schemas/money.ts       # zod schemas
```

**Server Actions in `apps/web/src/app/app/money/actions.ts`:**
- `listTransactions(filter)` — month + category filters, 200-row cap
- `listCategories()`
- `createTransaction(input)` — validates, auto-categorizes, inserts
- `deleteTransaction(id)` — user-scoped
- `bulkCreateTransactions(rows[])` — CSV batch import, max 1000 rows
- `createCategory(input)`
- `updateCategory(id, input)` — partial update
- `deleteCategory(id)` — transactions get SetNull, not deleted

## Known issues / deferred items

- **No page-level loading skeletons.** Button-level loading (via `useTransition`) works. Phase 7 will add `loading.tsx` per route.
- **Generic error messages** on some actions ("Something went wrong"). Phase 7 polish.
- **`confirm()` / `alert()` for deletes.** Phase 7 replaces with proper dialogs.
- **No 404/500 page customization.** Phase 7.
- **No tests yet.** Deferred to Phase 7 per plan (unit tests for categorizer are low-hanging fruit).
- **Some migrations might need to be re-deployed to prod.** User runs them manually from local. Phase 7 adds `prisma migrate deploy` to a deploy hook.

## Environment / secrets state

- Local `.env` has all 11 env vars set, Railway public DATABASE_URL
- Vercel env vars set for Production + Preview + Development
- GitHub OAuth app registered with callback: `https://tmmt-web.vercel.app/api/auth/callback/github`
- Google OAuth app registered (same pattern)
- `ANTHROPIC_API_KEY` **not yet set** — needed for Phase 2

## Next session's goal

**Start Phase 2 — Financial Intelligence.**

Per the plan doc, Phase 2 splits into ~5 PRs:

1. **AI categorizer fallback** — create `apps/web/src/lib/ai-categorizer.ts`. Claude tool-calling, runs when rules return `null`. Add `packages/shared/src/llm/provider.ts` interface. Update `createTransaction` and `bulkCreateTransactions` to use hybrid flow.
2. **Categorization metadata** — add `confidence` + `categorizationSource` enum fields to `Transaction`. Surface "AI-tagged" badge in UI.
3. **Subscription detection** — scheduled scan for recurring charges. This may need NestJS to run server-side cron — first Phase 4-ish work creeping in. Could also do as on-demand "Analyze subscriptions" button first.
4. **Budget tracking** — UI at `/app/money/budgets`. Set monthly limits per category. Progress bars.
5. **Anomaly detection + weekly summary** — statistical outlier flagging, BullMQ weekly job (needs worker).

**Blocker on #3 and #5:** they assume a running worker. May want to reorder: do AI categorizer (1, 2) + budgets (4) in Phase 2, push subscription detection + anomaly detection to Phase 2.5 or combine with Phase 4.

**Recommended first step next session:** ask the user if they want to kick off PR #1 of Phase 2 (AI categorizer). Before that, user needs to set `ANTHROPIC_API_KEY` in Vercel and local `.env`.

## Context highlights for next AI

- User has a Claude Max subscription (chat + API likely come out of same org `aakimm0110@gmail.com's Organization`)
- Session handoff pattern is "read CLAUDE.md + latest handoff-*.md, continue"
- User prefers pragmatic shortcuts over textbook purity (see: Server Actions vs NestJS REST decision)
- User is new to AI-agent end-to-end projects — explain architecture changes before implementing
- User's preferred git workflow is now muscle memory: branch → 3-gate review → squash merge → delete branch

## Top open questions (ask the user next session)

1. **AI provider key** — have you added `ANTHROPIC_API_KEY` to local `.env` and Vercel?
2. **Phase 2 ordering** — AI-first (PR #1-2) then budgets (PR #4), or budgets first then AI?
3. **NestJS activation** — Phase 2 could push NestJS into use for the subscription scanner cron. Or defer to Phase 4. Your call.
